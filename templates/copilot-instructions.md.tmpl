<!-- GÉNÉRÉ par tools/sync-ai → .github/copilot-instructions.md -->
# Copilot Instructions

> Copilot ne lit que `.github/` et n'importe pas de fichiers externes → le sync **inline** `global` +
> `method` + `meta` ici, et **matérialise** chaque `stacks/*` et `contexts/*` en
> `.github/instructions/<name>.instructions.md` (avec leur frontmatter `applyTo`).

{INLINE conventions/global.md}
{INLINE conventions/method.md}
{INLINE conventions/meta/architecture-principles.md}

<!-- stacks/ et contexts/ → générés séparément dans .github/instructions/ (scoping applyTo préservé) -->
